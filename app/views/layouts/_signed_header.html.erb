<nav id="mainNav" class="navbar navbar-default navbar-custom navbar-fixed-top ltt-navbar">
  <div class="container header-new">
    <div class="header-left navbar-header page-scroll col-md-7">
      <ul class="nav navbar-nav">
        <li id="title" class="navbar-brand page-scroll">
          <%= link_to t(:title), root_url, class: "navbar-brand page-scroll ltt-title" %>
        </li>
        <li class="header-search">
          <%= form_tag searches_path, method: :get do %>
            <span>
              <%= text_field_tag :search, params[:search],
                placeholder: "写真の情報を入力してください", class: "form-control
                images-search", id: "images-search" %>
              <i class="fa fa-search" aria-hidden="true"></i>
            </span>
          <% end %>
          <span class="search-results">
            <ul class="results">
            </ul>
          </span>
        </li>
      </ul>
    </div>
    <div class="collapse navbar-collapse col-md-5" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li class="hidden">
          <a href="#page-top"></a>
        </li>
        <li class="active ltt-header-item signed-in" id="_home">
          <%= link_to t(:home), root_path, class: "page-scroll" %>
        </li>
        <li class="ltt-header-item" id="_image_share">
          <%= link_to t(:image_share), popular_images_path, class: "page-scroll" %>
        </li>
        <li><%= link_to t("user.list-users"), users_path %></li>
        <li class="active ltt-header-item" id="_menu_action">
          <a href="#" class="dropdown-toggle page-scroll" data-toggle="dropdown"
            title="<%= current_user.name %>">
            <% if current_user.name.length > 15 %>
              <%= truncate current_user.name, length: 15 %>
            <% else %>
              <%= current_user.name %>
            <% end %>
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to t("profile"), user_path(current_user) %>
            </li>
            <li><%= link_to t("user.setting"), edit_user_path(current_user) %></li>
            <li>
              <%= link_to t("upload_image"), new_image_path %>
            </li>
            <li class="divider"></li>
            <li>
              <%= link_to t("sessions.sign_out"),
                destroy_user_session_path, method: :delete %>
            </li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>

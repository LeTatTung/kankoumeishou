<p id="notice"><%= notice %></p>

<h1>Listing Image Comments</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Image</th>
      <th>Comment</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comments.each do |image_comment| %>
      <tr>
        <td><%= image_comment.user_id %></td>
        <td><%= image_comment.image_id %></td>
        <td><%= image_comment.comment %></td>
        <td><%= link_to "Show", comment %></td>
        <td><%= link_to "Edit", edit_comment_path(image_comment) %></td>
        <td><%= link_to "Destroy", comment, method: :delete,
          data: { confirm: "Are you sure?" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to "New Image comment", new_comment_path %>
